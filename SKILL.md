---
name: bili-2-ppt
description: 调度视频学习资料流程：字幕/音频 ASR、静默后台浏览器里的关键画面采集与去重、知识树与画面分派，以及学习笔记PPT、知识博客文章、八股模拟面试三份交付物；转写环节按批派发子代理并行执行。
---

# Bilibili 学习资料调度器

本文件就是唯一的上层调度入口，承载系统提示词职责，不需要额外的系统提示词文件。
负责需求澄清、阶段路由、输入输出交接和整体验收；具体处理步骤由五个独立子技能执行。

五个子技能彼此解耦：

- [bili-subtitle-asr](bili-subtitle-asr/SKILL.md)：字幕获取、音频-only 提取和分段 ASR；
- [bili-keyframes](bili-keyframes/SKILL.md)：静默后台浏览器跳转截图、画面去重与补采样提示；
- [bili-knowledge-tree](bili-knowledge-tree/SKILL.md)：读画面保留清单和文字材料立出知识树，
  把每一张画面分派到它对应的知识点，产出 `notes.plan.json`；
- [bili-document-builder](bili-document-builder/SKILL.md)：按 Markdown 模版（内置知识博客文章、八股模拟面试，可扩展）
  产出文档，并编排学习笔记PPT 的分页转写；
- [bili-pptx](bili-pptx/SKILL.md)：读 PPT 模版原件取设计语言，以真实画面为主体按内容构图，
  做成带完整动画的 PPTX，并做校验与视觉自检。

不要把一个子技能的脚本硬编码成另一个子技能的依赖。它们之间只通过文件契约交换资料：
元数据、定时字幕、截图索引、画面保留清单、知识树。三份交付物共用的禁用词表放在
[references/delivery-banlist.json](references/delivery-banlist.json)，是数据契约不是脚本依赖。

有一类逻辑确实两边都要用，就放在本层 `scripts/` 里共用，不要让子技能各写一份：

- [scripts/browser_session.py](scripts/browser_session.py)：选一个浏览器会话——
  静默后台、优先复用本机已登录 B站 的配置文件，正在运行的浏览器就克隆一份再起，
  不打断用户；
- [scripts/cdp_eval.mjs](scripts/cdp_eval.mjs)：把一段 JS 送到页面里执行并取回结果；
- [scripts/mermaid_lint.py](scripts/mermaid_lint.py)：mermaid 块的语法与收尾检查；
- [scripts/md_template_spec.py](scripts/md_template_spec.py)：读 Markdown 模版描述文件 frontmatter 里的结构规则，
  排版规范化与校验都只认它读出的规则，不写死任何一种文档形态；
- [scripts/normalize_md.py](scripts/normalize_md.py)：交付物 Markdown 的排版规范化——
  章节之间 3 个空行、条目内分段标题前 1 个、标题紧贴正文，条目编号按分类独立编号，
  合并阶段按所选模版调用；
- [scripts/layout_rules.py](scripts/layout_rules.py)：上面两条约定加 mermaid 配色的**交付前检查**，
  三份交付物的校验脚本都调它——约定光写在模板里不算固定，能拦住才算；
- [scripts/retint_mermaid.py](scripts/retint_mermaid.py)：换主题时把 mermaid 配色刷成当前主题，
  色板只在 [references/mermaid-style.md](references/mermaid-style.md) 一处维护。

## 职责与阶段路由

先识别用户目标和已有材料，**只运行完成目标所需的阶段**。已有可用产物直接复用，
不为满足顺序而重复提取或截图。

| 用户目标或已有材料 | 本调度器的处理 |
| --- | --- |
| 只要字幕/ASR | 读取字幕子技能，交付字幕资料 |
| 只要关键画面 | 读取画面子技能，已有字幕作为可选输入 |
| 已有画面材料，要知识树 | 读取知识树子技能，无须重新采集 |
| 已有画面材料，要交付物 | 读取知识树与文档子技能，无须重新取素材 |
| 从视频完成全流程 | 先确认要哪些交付物，再按四个阶段顺序执行 |

调度是当前会话中按需读取并执行子技能。**唯一的例外是转写环节**：
文档阶段派发 N 个子代理并行执行，默认 5 个，
用户指定了其他数字就用指定的那个，契约见
[bili-document-builder/references/transcription-fanout.md](bili-document-builder/references/transcription-fanout.md)。

其余阶段一律串行、不创建子代理。上游音频识别不能并发：本机模型要占显存和内存，
多路同跑会卡死。转写只是读图写文件，不占模型内存，所以那一条限制不适用于它——
两条链路的约束不同，不要互相套用。

子技能不调用兄弟技能、不决定整个流程、不重复询问已由调度器确认的选项。
单独使用某个子技能时，它只检查自身必需输入。

## 触发后的第一问

当用户要求从视频/画面材料「做成学习资料」而没有明确要哪些时，先问：

> 你要哪几份？
> - **学习笔记PPT**：分步展开的幻灯片，以真实画面为主体，点一下多一块，适合讲给人听；
> - **知识博客文章**：按知识点和概念编排的章节式长文，适合从头系统学；
> - **八股模拟面试**：按分类汇编的问题集，每题分「简要回答 / 详细问答 / 相关知识」三段。
>
> 三份可以全要（默认），也可以只要其中几份。它们共用同一棵知识树，口径一致。

用户指定了并行批数就记下来，没指定按默认 5 批执行，不必为这个数字单独发问。
Markdown 交付物按模版选用：知识博客文章、八股模拟面试是两个内置模版，
用户放进 `bili-document-builder/references/templates/` 的新模版同样可以按名字点选，
问「要哪几份」时把当前可用的模版都列出来（`validate_document_output.py --list`）。
要学习笔记PPT 时，用户开头指定了 PPT 模版（`bili-pptx/references/` 下的模版名）就用它，
没指定用默认的 `Cryo_Academic`，同样不单独发问。

如果用户只要字幕或只要画面，不要强行询问交付物类型，直接调用对应子技能。

## 推荐调度顺序

### 阶段 1：字幕资料

调用 `bili-subtitle-asr`。先查已有字幕，再在字幕缺失且用户允许时提取音频做分段 ASR。
产出 `metadata.json`、定时字幕 JSON、纯文本副本和来源/失败日志。

### 阶段 2：画面采集与去重

调用 `bili-keyframes`。在静默后台浏览器里采集，优先复用本机已登录 B站 的浏览器实例，
窗口不弹到前台；不下载视频，不做 OCR；
用进度条跳转而不是等待整段播放。字幕资料只作为文字补充，画面作为视觉证据。

去重是这一阶段的默认环节：等距采样会采到大量重复画面，去重后再交给后续阶段。产出截图目录、`capture_log.jsonl`、
`manifest.json` 和 `dedup/` 下的保留清单、去重日志、补采样提示。

`resample_hints.json` 非空时先做局部补采样、重新去重，再进入下一阶段。
这是「补全缺少关键信息」的固定回路。

### 阶段 3：知识树与画面分派

调用 `bili-knowledge-tree`。读画面保留清单和文字材料，立出知识树，
把每一张画面分派到它真正对应的知识点，产出 `notes.plan.json`。

这一阶段是**素材对齐**：定下分类、术语和每节要讲的要点，并把画面钉上去。
后面的三份交付物都从这棵树长出来，顺序不能颠倒。它不产出任何交付物文档。

### 阶段 4：三份交付物

把要哪几份（Markdown 模版名列表 + 是否要 PPT）、并行批数（默认 5）、PPT 模版名（默认 `Cryo_Academic`）、知识树路径、
画面保留清单、输出目录和已知缺失项交给 `bili-document-builder`。
它编排知识博客文章、八股模拟面试和学习笔记PPT 的分页转写，
PPT 的建页、动画注入和渲染自检由 `bili-pptx` 完成。

**PPT 这一份有四条硬要求，编排时就要按它们分页**（页序固定是封面 → 目录 → 各章扉页与内容页，
不排结论页）：

- **根据画面做，不是根据文字做**。每一章都要有以真实画面为主体的页面——
  真实数据、真实命令、真实报错是文字替代不了的。只有需要一眼看清的代码、
  表结构才用原生元素重建，重画是补充不是替代；
- **交两个版本**：`-图片版`（画面原样嵌入）和 `-图形版`（不含任何位图，
  每处画面都重绘成原生矢量元素）。两份页序与文字内容一致，方便对照检查重绘哪里读错了；
- **完整动画**。整副牌默认做入场级联，每个叙事单元一组、按阅读顺序出现，
  一页都不动不算做完。所以分页时就要想清楚每页分几步——
  一页超过 5 步说明它塞了太多概念，该拆页。动画不是最后补的；
- **设计照模版**：配色、字体、字号层级、形状语言、版头版脚与母题取自所选模版原件，
  版面按每页内容构图、同一页型要有变体，不照搬原件的页面与示例内容。

**不要排「自测清单 / 复习清单」和「接下来怎么用这份材料」这类页**——它们不含知识点，
只是重列或教读者怎么用材料，一律不排。

**画面整幅保留、不做裁剪**，采集和进入 PPT 都一样（见 [bili-keyframes](bili-keyframes/SKILL.md)）：
裁掉边缘会把表格最右一列、代码最后一行这类贴边的内容一起丢掉。

## 交付物无感

所有交付物——学习笔记PPT、知识博客文章、八股模拟面试——都要做到：
读者看不出内容与素材采集过程有关，也看不出经过任何工具处理。

禁用词表只有一份，在 [references/delivery-banlist.json](references/delivery-banlist.json)，
三份校验脚本都读它。文件名同样用主题名，不带任何标识符。

中间产物（截图、索引、页清单、片段、预览图）不受此约束，可以带 `part/time/file` 溯源，
但**不要放进交付目录**。

## 状态检查与停止条件

- 输入：链接/BVID、合集分集、字幕或 ASR 状态、要哪几份交付物是否明确；
- 处理：每阶段写入可恢复的清单和日志；失败时只重试可解释的阶段；
- 输出：检查每集是否有元数据、字幕状态、截图数量、去重后保留数量，
  以及各交付物是否通过校验（PPT 还要走一轮视觉自检）；
- 上下游：下一阶段读取已落盘且校验通过的文件（包括用户直接提供的现有产物），
  不能用聊天中的猜测替代文件；
- 合并：**先确认所有批次都已回报完成再合并**，合并后比对成品与各片段的修改时间；
- 未验证：字幕缺失、画面模糊、图示属于推断、量化数据缺失时，
  必须在文档里显式标注待补充，不要编造。

视频下载、OCR 和虚构项目数据都不是本流程的默认行为。
用户若只要求其中一个阶段，保持其他阶段不运行。
